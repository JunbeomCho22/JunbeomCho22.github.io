  <nav class="nav__list">
    <input id="ac-toc" name="accordion-toc" type="checkbox" />
    <label for="ac-toc">토글 메뉴</label>
    <ul class="nav__items" id="category_tag_menu">
        <li>
            📂 카테고리
        </li>
        <li>
            <span class="nav__sub-title" style="margin: 0; margin-left: 10px; cursor: pointer;" onclick="toggleAzure()">Azure</span>
            <ul id="azureSubMenu" style="display: none;">
                {% for category in site.categories %}
                    {% if category[0] == "azure" %}
                        <li><a href="/categories/azure" class="" style="margin-left: 20px;">Azure ({{ category[1] | size }})</a></li>
                    {% endif %}
                {% endfor %}
            </ul>
        </li>
        
        <script>
            function toggleAzure() {
                var subMenu = document.getElementById("azureSubMenu");
                if (subMenu.style.display === "none") {
                    subMenu.style.display = "block";
                } else {
                    subMenu.style.display = "none";
                }
            }
        </script>
    </ul>
  </nav>